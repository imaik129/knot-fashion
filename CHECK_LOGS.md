# 📋 ログ確認ガイド

## ログファイルの場所

### バックエンドログ
**場所:** `/tmp/backend.log`

### フロントエンドログ
**場所:** `/tmp/frontend.log`

## 便利なコマンド

### バックエンドログを確認

```bash
# 最後の50行を表示
tail -50 /tmp/backend.log

# 最後の100行を表示
tail -100 /tmp/backend.log

# リアルタイムで監視（Ctrl+Cで停止）
tail -f /tmp/backend.log

# エラーのみを表示
grep -i error /tmp/backend.log | tail -20

# エラーとその周辺の行を表示
grep -i error /tmp/backend.log -A 5 -B 5 | tail -50

# 最新のPOSTリクエストを確認
grep "POST /process-scan" /tmp/backend.log | tail -10
```

### フロントエンドログを確認

```bash
# 最後の50行を表示
tail -50 /tmp/frontend.log

# リアルタイムで監視
tail -f /tmp/frontend.log

# エラーのみを表示
grep -i error /tmp/frontend.log | tail -20
```

## よくあるエラーの確認方法

### ROMP初期化エラー
```bash
grep -i "ROMP\|initialized\|ERROR" /tmp/backend.log | tail -20
```

### ビデオ処理エラー
```bash
grep -i "process-scan\|Failed\|Exception" /tmp/backend.log | tail -30
```

### すべてのエラーを確認
```bash
grep -E "ERROR|Error|Exception|Traceback|Failed" /tmp/backend.log | tail -50
```

## ログをクリアする

```bash
# バックエンドログをクリア
> /tmp/backend.log

# フロントエンドログをクリア
> /tmp/frontend.log
```

## ブラウザのコンソール

フロントエンドのエラーは、ブラウザの開発者ツールでも確認できます：

1. ブラウザで `F12` または `Cmd+Option+I` (Mac) を押す
2. **Console** タブを開く
3. エラーメッセージを確認

## 現在のログを確認

```bash
# バックエンドの最新エラー
tail -100 /tmp/backend.log | grep -i error | tail -10

# フロントエンドの最新エラー
tail -100 /tmp/frontend.log | grep -i error | tail -10
```


